<p style="color: green"><%= notice %></p>

<%= render @project %>
<div>
<% if admin_mode? %>
<%= link_to "Back to projects", projects_path(key: ENV["ADMIN_KEY"]) %>

<%= button_to "Destroy this project", @project, method: :delete %>
<% end %>
<%= link_to "Back to projects", projects_path %>
</div>